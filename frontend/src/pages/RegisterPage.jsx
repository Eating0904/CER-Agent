import { RegisterCard } from '../features/user/RegisterCard';

import { Template } from './Template';

export const RegisterPage = () => (
    <Template>
        <RegisterCard />
    </Template>
);
