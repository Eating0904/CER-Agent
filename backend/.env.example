# Django Settings
SECRET_KEY=
DEBUG=True
ALLOWED_HOSTS=*
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Database Settings (Django 系統資料庫)
DB_HOST=postgres
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=db

# LangGraph Database Settings (獨立資料庫，用於對話持久化)
# 注意：本機執行時使用 localhost，Docker 內執行時使用 postgres
# 格式：postgresql://使用者:密碼@主機:埠號/資料庫名稱
DATABASE_URL=postgresql://postgres:DB_PASSWORD@localhost:5432/langgraph

# Google Gemini API Settings
GOOGLE_API_KEY=